package MyPackage;

public class Colt extends Horse {
    // because of extends
    // must use the modifiers with "public", otherwise it will go wrong
    public String ride() {
        return "tolting";
    }
}
